export const number = 1234

export function add(x, y) {
    return x + y
}

export function subtract(x, y) {
    return x - y
}

//export {number, add, subtract}
//export default {number, add, substract} => is the most common way of exporting 
//with default export in the import file add the code at the top of the page like this:
//import Steph from ".file1.js"
//note: Steph can be named anything